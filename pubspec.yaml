name: flint_client
description: A powerful, type-safe Dart client with HTTP, WebSocket, caching, retry, and AI provider support.
version: 0.0.3
repository: https://github.com/flint-dart/flint-client
homepage: https://flintdart.eulogia.net
documentation: https://github.com/flint-dart/flint-client#ai-support
issue_tracker: https://github.com/flint-dart/flint-client/issues
example: https://github.com/flint-dart/flint-client-sample
environment:
  sdk: ^3.8.1

dependencies:
  path: ^1.9.0
  clock: ^1.1.2
  web: ^1.1.1

dev_dependencies:
  lints: ^5.0.0
  test: ^1.24.0

# Project metadata

